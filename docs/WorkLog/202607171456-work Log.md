Step: Create the point-address table
File: migrations/0005_create_point_addresses.sql
What it does: Creates independent landing addresses and associates existing marker codes with their current points.
Checkpoint: The new migration file exists and the old URLs have a migration path.

---------------

Step: Apply address-table migration locally
Command: npx wrangler d1 migrations apply event_logger_db --local
What it does: Creates point_addresses in the local D1 database without changing the live database.
Checkpoint: Wrangler reports the migration as successful.

---------------

Step: Verify address associations
Command: D1 SELECT joining addresses to points
What it does: Confirms that landing addresses are separate records but still resolve to their points.
Checkpoint: Each row shows an address_code and the point_label it serves.

-----------------

Step: Route landing addresses to points
File: src/index.js
What it does: /m/{address} now looks up an address association and then loads its point.
Checkpoint: The QR address and point identity are no longer the same field.

---------------

Step: Test address-based routing
Command: npm test -- --run
What it checks: Whether the Worker can still create, resolve, display, and log to points after separating addresses.
Checkpoint: Either all tests pass or we get the exact next failure to fix.

-----------------


Step: Repair point lookup
What it does: The API extracts the landing address first, then resolves that address into a point.
Checkpoint: No duplicate markerCode syntax error.

--------------

Step: Create the address API
Route: POST /api/point-addresses
What it does: Creates an independently managed landing address associated with an existing point.
Checkpoint: The Worker now has a process for creating addresses, but the test does not call it yet.

--------------

